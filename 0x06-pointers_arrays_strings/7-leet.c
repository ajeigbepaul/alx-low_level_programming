#include "main.h"

/**
 * leet - Encode a string 
 * @str: The string to be encoded.
 *
 * Return: A pointer to the encoded string
 */

char *leet(char *str)
{
	int indx1 = 0, indx2;
	char leet[0] = {};

	while (str[index])
	{
		for (indx2 = 0; indx2 <= 1; inx2++)
		{
			if (str[indx1] == leet[indx2] || str[indx1] - 1 == leet[indx2])
		}
		index++;
	}
	return (str);
